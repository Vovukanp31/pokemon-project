import React from 'react';

const EvolutionCard = ({id}) => {
    console.log(`evolutionID -- ${id}`)
    return (
        <div>
            {id}
        </div>
    );
};

export default EvolutionCard;